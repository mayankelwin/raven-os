/**
 * RavenDevLayout — Native Stub
 * On mobile, layout is transparent (just renders children).
 */
import React from 'react';

interface Props { children: React.ReactNode; }
export const RavenDevLayout = ({ children }: Props) => <>{children}</>;
